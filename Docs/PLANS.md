# Plans

## 목적

이 문서는 현재 진행 중인 단계 상태와 최근 작업 흐름을 빠르게 확인하기 위한 기록 문서입니다.

<!-- 새 프로젝트 시작 시 작업리스트와 최근 작업 로그의 이전 프로젝트 기록을 초기화한다. -->

## 작업리스트

`Task 문서`가 `미생성`인 단계는 구현을 시작하지 않습니다. 사용자가 `Tasks/MAKE_REQUEST.md` 양식으로 요청하면 다음 순서에 맞는 단계 하나를 확정하고 task 문서를 생성합니다.

| 단계 | Task 문서 | 해야 할 항목 | 상태 |
|---|---|---|---|
| 01-1 | 미생성 | Aeterna Unreal 프로젝트 생성과 기본 실행 기준 확인 | 예정 |
| 01-2 | 미생성 | M-05 1인칭 이동과 시야 조작 검증 | 예정 |
| 01-3 | 미생성 | 경비실 시작 기준점과 최소 이동 검증 구역 구성 | 예정 |
| 01-4 | 미생성 | 헤드램프(F) ON/OFF와 플레이어 배터리 소모·충전(E) 검증 | 예정 |
| 01-5 | 미생성 | 배터리 잔량-헤드램프 밝기 연속 감쇠(강도·도달 거리·색온도) 검증 | 예정 |
| 02-1 | 미생성 | 01:00~05:00 진행과 특정 시각 이벤트 브로드캐스트를 담당하는 GameClock 검증 | 예정 |
| 02-2 | 미생성 | S01~S03을 DataTable에서 읽어 시작·클리어·게임 오버·재시작을 관리하는 ScenarioManager 검증 | 예정 |
| 02-3 | 미생성 | 현재 밤의 수첩 규칙과 업무 목표 표시 검증 (Tab, DataTable 경유) | 예정 |
| 02-4 | 미생성 | 수첩 규칙 위반 감지 후 현재 밤만 초기화하고 경비실·01:00으로 재기동하는 흐름 검증 | 예정 |
| 02-5 | 미생성 | 현재 밤 초기화 상태와 전체 진행 유지 상태 경계 검증 | 예정 |
| 02-6 | 미생성 | 밤 클리어 시 완료 지점 페이드아웃 →「Day N」 카드 → 다음 밤 재기동 전환 검증 | 예정 |
| 03-1 | 미생성 | GDD §6 NIGHT 1 기준 순찰 지점 A/B/C 배치 확정 (GDD §10 #6 결정 필요) | 예정 |
| 03-2 | 미생성 | S01의 순찰 업무와 목표 진행 흐름 검증 | 예정 |
| 03-3 | 미생성 | S01의 수첩 규칙 표시와 스캔·필사 흐름 검증 | 예정 |
| 03-4 | 미생성 | S01 시작부터 「Day 1」 카드까지 수직 조각 통합 검증 | 예정 |
| 04-1 | 미생성 | S01 검증 결과를 근거로 시나리오 제작 형식과 공통화 범위 확정 | 예정 |

## 후속 확장 원칙

- S02, S03은 GDD §6에 기획이 확정되어 있으므로, S01 수직 조각 검증이 끝난 뒤 하나씩 작업리스트에 추가합니다.
- 각 밤은 기획 반영, 겉 업무, 규칙·이상 현상, 클리어·실패 통합을 필요에 따라 작은 task로 나눕니다.
- S01에서 실제로 검증되지 않은 데이터 구조나 공통 시스템을 S02~S03용으로 선제 확정하지 않습니다.
- GDD §11의 확장 시나리오는 **밤 3개 완성 전에는 작업리스트에 넣지 않습니다** (스코프 방어).
- GDD §10 `결정 대기` 항목에 걸리는 단계는 결정이 끝나기 전에 구현을 시작하지 않습니다.
- 이전 task의 사용자 결과 확인이 끝나기 전에는 다음 단계로 넘어가지 않습니다.

## 상태 범례

- `예정`: 아직 task 문서가 없거나 구현 시작 전
- `진행중`: 구현 중이거나, 구현 후 사용자 수동 작업 또는 결과 확인을 기다리는 상태
- `완료`: 구현과 현재 확인 모드 기준 결과 확인이 완료된 상태

## 최근 작업 로그

- 2026-08-12: Aeterna용 하네스 문서 초기화. 8개 30분 시나리오, 경비실 시작·종료, 현재 시나리오 롤백, 수첩 중심 규칙 전달 기준을 초기 계획에 반영. 구현은 시작하지 않음.
- 2026-08-14: GDD v1.3을 단일 원본으로 전체 문서 정합화. 8개 30분 슬롯 → **밤 3개(S01~S03), 각 밤 01:00~05:00 전체**로 교체. 클리어 시 경비실 복귀 → **완료 지점 페이드아웃 →「Day N」카드**로 교체. 고정 담당제(GDD §9) 채택, 레벨 네이밍 `Museum_Persistent`/`Sub_*` 통일, `Docs/` 경로 대소문자 정정. 작업리스트를 새 구조로 갱신. 구현은 시작하지 않음.
- 2026-08-14: GDD §3-2에 **잔량-밝기 연동** 확정 반영 (강도·도달 거리·색온도 연속 감쇠). §10 #3 튜닝 항목에 감쇠 곡선 추가, #10을 하강 나선 리스크로 확장·우선순위 상향. 단계 01-5 추가.

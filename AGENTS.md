# AGENTS.md — 에테르나 (Aeterna)

> 이 문서는 AI 코딩 에이전트와 팀원이 이 저장소에서 작업할 때 따라야 하는 규칙이다.
> 기획의 원본은 `Docs/GDD_v1.3_에테르나.md`, 작업 절차는 `Docs/WORKFLOW.md` — 충돌 시 GDD가 우선한다.
> 전체 문서 지도는 §9 참조.

---

## 1. 프로젝트 개요

- **게임**: 에테르나 — 1인칭 규칙 기반(나폴리탄 괴담) 호러 어드벤처
- **엔진**: Unreal Engine **5.6** (버전 임의 업그레이드 금지 — 팀 합의 필요)
- **구성**: 밤(시나리오) 3개. 밤 완료 시 페이드아웃 → 「Day N」 카드 → 다음 밤 경비실 재기동
- **플레이어**: 야간 관리 로봇 M-05. 내장 헤드램프(F), 플레이어 배터리(맵의 배터리에 E로 충전) — 배터리는 **본체 전원 + 헤드램프 전원 겸용**이며, 잔량이 줄면 헤드램프가 연속적으로 어두워진다 (GDD §3-2)
- **핵심 규칙**: "수첩에 적힌 내용과 상이한 행동 = 게임 오버(해당 밤 재시작)"가 유일한 실패 조건

## 2. 팀 및 작업 조율

팀원: **찬진, 정준, 지수, 설화** (4인). **고정 담당제** — 담당 영역은 GDD §9를 따른다.

| 담당 | 범위 | 담당자 |
|---|---|---|
| A. 시스템 | GameClock, ScenarioManager, FailureSystem, 세이브 | 미배정 |
| B. 플레이어 | M-05 조작(5입력), 헤드램프·배터리, 스캔, 수첩·UI 에셋 통합 | 미배정 |
| C. 레벨 | 서브레벨, 라이팅(점등 스케줄 포함), 경비실·배전실 프롭 | 미배정 |
| D. 시나리오 | 밤1~3 스크립팅, 그림자 시스템, 사운드, 실패/클리어 연출 | 미배정 |

> 담당자 4명 ↔ 영역 A~D 배정은 GDD에 없다. 팀이 정한 뒤 이 표의 `담당자` 칸을 채운다.

**서브레벨 1담당제** (GDD §8): `Sub_*` 서브레벨은 각각 담당자 1명이 소유한다. 소유자가 아닌 사람은 해당 서브레벨을 직접 수정하지 않는다.

조율 규칙:

1. **자기 담당 영역 안에서 작업한다.** 다른 담당 영역의 파일을 수정해야 하면 그 영역 담당자와 먼저 합의한다.
2. 같은 담당 영역 안에서도 작업 시작 전 **한 줄 선언**한다: "저 중앙홀 맵 잡습니다", "GameClock C++ 수정 중" 등
   - 확인 수단: 팀 채널 `#지금-작업중` 선언, `git lfs locks` (누가 어떤 에셋을 잠갔는지), 최근 커밋 로그
3. 담당 경계에 걸치는 기능(예: 배터리 UI = B, 배터리 소모 판정 = A)은 **양쪽 담당자가 인터페이스를 먼저 합의**한 뒤 각자 구현한다.
4. AI 에이전트도 동일: 잠긴 에셋, 타인 담당 영역, 타인이 작업 중이라고 선언된 영역은 **수정 제안만 하고 직접 수정하지 않는다**.

## 3. Aeterna 기획 기준 (디자인 불변 조건 — 에이전트가 절대 위반하면 안 되는 것)

1. **조작은 5입력뿐**: WASD / Shift / Tab(수첩) / E(상호작용·충전) / F(헤드램프). 점프·웅크리기·인벤토리 등 신규 입력을 추가하는 코드를 작성하지 않는다.
2. **게임 오버 조건은 수첩 규칙 위반뿐**. 새로운 사망/실패 경로를 임의로 추가하지 않는다. (배터리 방전의 의미는 GDD §10 결정 대기 — 확정 전 구현 금지)
3. **전시물이 움직이는 순간을 보여주는 연출 금지** — 변화는 항상 플레이어 시야 밖에서만 (시야 감지 유틸 사용).
4. **위반 직전 경고 UI 금지** — 판단은 플레이어 몫이라는 디자인 기둥을 지킨다.
5. 밤 종료는 **완료 지점 페이드아웃 → 「Day N」 카드**. 경비실 복귀 로직을 만들지 않는다.
6. 규칙·시각 이벤트·실패 조건은 **DataTable(CSV) 기반**. 시나리오 내용을 C++/BP에 하드코딩하지 않는다.

## 4. 코드 규칙

### 4-1. C++ / Blueprint 경계
- **C++ (코어)**: GameClock, ScenarioManager, FailureSystem, NotebookRules, 시야 감지, 배터리, 세이브
- **BP (연출)**: 시나리오 이벤트 트리거, 라이팅/사운드 연출, UI 애니메이션
- 새 기능이 어느 쪽인지 애매하면: "판정·상태·저장 = C++, 보여주기 = BP"

### 4-2. 네이밍
- C++ 클래스: 언리얼 표준 접두 (`A`, `U`, `F`, `E`) + `Aeterna` 모듈 접두 없이 도메인명 (`UGameClockSubsystem`, `UScenarioManager`)
- BP: `BP_` / 위젯: `WBP_` / 애님 BP: `ABP_` / 데이터테이블: `DT_` / 데이터 에셋: `DA_` / 머티리얼: `M_`, `MI_`
- 레벨: 퍼시스턴트 = `Museum_Persistent`, 서브레벨 = `Sub_` 접두 (`Sub_GrandHall` 등). **`L_` 접두는 사용하지 않는다.**
- 시나리오 ID는 **`S01`~`S03` 3개로 확정** (밤 3개). 확장 시나리오는 GDD §11 — 밤 3개 완성 전에는 ID를 늘리지 않는다.
- 시나리오 에셋: `S01_`, `S02_`, `S03_` 접두 (예: `BP_S02_FossilShadow`, `DT_S03_LightSchedule`)
- 여러 밤이 공유하는 에셋에는 시나리오 ID를 붙이지 않는다
- 태그·ID는 영문 스네이크/파스칼 일관 (예: `Patrol_A`, `Light_Emergency_Red_01`)

### 4-3. 데이터
- 기획 데이터는 구글 시트 → CSV export → `Content/Aeterna/Data/` 임포트
- DataTable 구조체(스키마) 변경은 팀 전체 공유 후 진행 — 스키마가 깨지면 모든 시나리오가 깨진다
- 텍스트(수첩·로그·자막)는 String Table 사용 (하드코딩 금지)

## 5. Git 규칙

> 아래 규칙은 **Unreal 프로젝트 저장소**(`Aeterna.uproject`가 있는 Git + LFS 저장소)에 적용된다.
> 기획·하네스 문서만 있는 폴더에서는 `git lfs` 절차가 적용되지 않는다 — 해당 폴더에서 작업할 때는 lock 확인을 건너뛰고 문서 편집 규칙만 따른다.

1. **작업 시작 전 pull, 바이너리 에셋 작업 전 lock** (`git lfs lock <path>`), push 후 unlock
2. `.uasset`/`.umap`은 머지 불가 — **잠그지 않은 바이너리는 수정하지 않는다** (AI 에이전트 포함)
3. 커밋 접두어: `[C++]` `[BP]` `[Map]` `[Data]` `[Doc]` — 작게 자주 커밋
4. `Binaries/`, `Intermediate/`, `Saved/`, `DerivedDataCache/` 커밋 금지
5. main은 항상 에디터가 켜지는 상태 유지 — 컴파일 에러/빨간 BP 상태로 push 금지
6. 폴더 이동 시 Fix Up Redirectors 필수
7. 브랜치: `main` + 단명 `feature/*` (1~3일 내 머지). 바이너리 포함 브랜치는 최대한 짧게

## 6. 디렉터리 구조

```
Aeterna/
├─ Source/Aeterna/          # C++ 코어
├─ Content/Aeterna/
│  ├─ Maps/                 # Museum_Persistent + Sub_*
│  ├─ Blueprints/
│  │  ├─ Player/
│  │  ├─ Scenario/S01~S03/  # 밤별 전용 BP
│  │  └─ Core/              # 밤 공용 BP
│  ├─ Data/                 # DT_*, CSV
│  ├─ UI/                   # WBP_*
│  ├─ Audio/
│  └─ VFX/
├─ Docs/                    # 기획 원본 + 하네스 문서 (§9)
├─ Tasks/                   # task 문서 및 요청 양식
├─ AGENTS.md
└─ CLAUDE.md
```

> `Source/`, `Content/` 하위 경로는 **실제 task에서 필요할 때 생성**한다. 계획에 있다는 이유만으로 빈 폴더를 미리 만들지 않는다 (`Docs/ARCHITECTURE.md`).

## 7. AI 에이전트 작업 지침

- 구현 전 반드시 `Docs/GDD_v1.3_에테르나.md`의 해당 섹션을 확인하고, GDD에 없는 기능은 **"GDD에 없음 — 팀 확인 필요"** 를 명시한 뒤 제안만 한다.
- §3 불변 조건과 충돌하는 요청을 받으면 충돌 사실을 알리고 대안을 제시한다.
- UE 5.6 API 기준으로 작성한다. 5.0~5.3에서 deprecated된 API를 사용하지 않는다.
- 바이너리 에셋(.uasset/.umap)은 생성·수정 절차만 텍스트로 안내하고 직접 조작을 시도하지 않는다.
- 시나리오 텍스트(수첩 규칙, 시스템 로그)를 수정할 때는 기존 문체를 유지한다: 수첩 = 낡은 손글씨 경어체("~하시오/~마시오"), 시스템 로그 = 기계식 단문(`NIGHT EXHIBIT CHECK: ACTIVE`).
- 결정 대기 항목(GDD §10)에 해당하는 구현 요청은 결정이 필요함을 먼저 알린다.

## 8. Definition of Done

- [ ] 에디터에서 컴파일 에러·BP 에러 없음
- [ ] 해당 밤을 처음부터 플레이해 기능 확인 (게임 오버 → 재기동 경로 포함)
- [ ] GDD와 동작이 다르면 GDD를 갱신하거나 코드를 고침 (문서-코드 불일치 금지)
- [ ] 커밋 접두어·메시지 규칙 준수, lock 해제 완료
- [ ] 타인 담당 영역(§2)을 건드렸다면 그 담당자의 리뷰 완료

---

## 9. 문서 지도

| 문서 | 역할 | 언제 읽나 |
|---|---|---|
| `Docs/GDD_v1.3_에테르나.md` | **기획 원본.** 모든 문서와 충돌 시 최우선 | 구현 전 해당 섹션 필수 |
| `AGENTS.md` (이 문서) | 저장소 규칙 진입점 | 항상 |
| `CLAUDE.md` | Claude Code 전용 추가 지침 | Claude Code 사용 시 |
| `Docs/WORKFLOW.md` | 작업 순서, 진행 제약, 승인 양식 | task 구현 시작 시 |
| `Docs/PLANS.md` | 현재 단계 상태와 최근 작업 로그 | 세션 시작 시 |
| `Docs/ARCHITECTURE.md` | 폴더 책임, 시스템 책임, 상태 소유권 | 파일 생성·이동 시 |
| `Docs/STYLEGUIDE.md` | C++/BP/에셋/주석 규칙 | 코드 작성 시 |
| `Docs/TESTING.md` | 검증 기준과 실행 확인 모드 | 구현 후 |
| `Docs/TASK_WRITING.md` | task 문서 작성·갱신 규칙 | task 문서 작성 시 |
| `Docs/HARNESS.md` | 규칙 운영·이탈 대응 절차 (메타) | 규칙을 고칠 때 |
| `Docs/RULE_ORIGINS.md` | 규칙별 출처 장부 | 규칙을 고칠 때 |
| `Docs/AUDIT_LOG.md` | 문서 정합성 감사 체크리스트 | 감사 요청 시 |
| `Docs/PENDING_ISSUES.md` | 미해소 구조 의심 | 구현 후 정리 시 |
| `Docs/LOGS.md` | PLANS에서 밀려난 과거 로그 | 이력 조회 시 |
| `Tasks/template.md`, `Tasks/MAKE_REQUEST.md` | task 문서 양식 / 생성 요청 양식 | task 생성 시 |

세션 시작 시 읽는 순서: `AGENTS.md` → `Docs/WORKFLOW.md` → `Docs/PLANS.md`.
